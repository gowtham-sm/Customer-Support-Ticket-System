<Window x:Class="SupportTicketClient.CreateTicketWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Create New Ticket" Height="550" Width="600"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular" TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal" TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Border Background="{DynamicResource MaterialDesignPaper}" CornerRadius="12" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="4" Opacity="0.2" Direction="270"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="Transparent" MouseDown="DragWindow_MouseDown">
                <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignFlatButton}" Width="40" Height="40" Padding="0" Click="BtnClose_Click" Foreground="{DynamicResource MaterialDesignBody}">
                    <materialDesign:PackIcon Kind="WindowClose" Width="20" Height="20"/>
                </Button>
            </Grid>

            <StackPanel Grid.Row="1" Margin="30,10,30,30">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,25">
                    <materialDesign:PackIcon Kind="TicketConfirmationOutline" Width="30" Height="30" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Submit a Support Ticket" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>

                <TextBox x:Name="txtSubject" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="Subject *" Margin="0,0,0,20" FontSize="16"/>
                <ComboBox x:Name="cmbPriority" Style="{StaticResource MaterialDesignFloatingHintComboBox}" materialDesign:HintAssist.Hint="Priority *" Margin="0,0,0,20" FontSize="16" SelectedIndex="1">
                    <ComboBoxItem Content="Low"/>
                    <ComboBoxItem Content="Medium"/>
                    <ComboBoxItem Content="High"/>
                </ComboBox>
                <TextBox x:Name="txtDescription" Style="{StaticResource MaterialDesignOutlinedTextBox}" materialDesign:HintAssist.Hint="Description *" Height="120" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,30"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="CANCEL" Style="{StaticResource MaterialDesignOutlinedButton}" Width="100" Margin="0,0,15,0" Click="BtnCancel_Click"/>
                    <Button x:Name="btnSubmit" Content="SUBMIT" Style="{StaticResource MaterialDesignRaisedButton}" Width="120" Click="BtnSubmit_Click"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>