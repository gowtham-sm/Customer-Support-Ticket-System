<Window x:Class="SupportTicketClient.TicketDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Ticket Details" Height="800" Width="1080"
        WindowStartupLocation="CenterOwner" Loaded="Window_Loaded"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular" TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal" TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Border Background="{DynamicResource MaterialDesignPaper}" CornerRadius="12" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="4" Opacity="0.2" Direction="270"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="Transparent" MouseDown="DragWindow_MouseDown">
                <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignFlatButton}" Width="40" Height="40" Padding="0" Click="BtnClose_Click" Foreground="{DynamicResource MaterialDesignBody}">
                    <materialDesign:PackIcon Kind="WindowClose" Width="20" Height="20"/>
                </Button>
            </Grid>

            <Grid Grid.Row="1" Margin="20,0,20,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1.5*"/>
                </Grid.ColumnDefinitions>

                <ScrollViewer Grid.Column="0" Margin="0,0,20,0" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
                    <StackPanel>
                        <materialDesign:Card materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="8" Margin="0,0,0,20">
                            <StackPanel>
                                <materialDesign:ColorZone Mode="PrimaryMid" Padding="16" CornerRadius="8,8,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="InformationOutline" Width="24" Height="24" Foreground="{DynamicResource PrimaryHueMidForegroundBrush}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="Ticket Information" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidForegroundBrush}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </materialDesign:ColorZone>

                                <StackPanel Margin="20">
                                    <TextBlock Text="Ticket #" FontSize="12" FontWeight="SemiBold" Opacity="0.68" Margin="0,0,0,2"/>
                                    <TextBlock x:Name="lblTicketId" FontSize="18" FontWeight="Medium" Margin="0,0,0,15"/>

                                    <TextBlock Text="Subject" FontSize="12" FontWeight="SemiBold" Opacity="0.68" Margin="0,0,0,2"/>
                                    <TextBlock x:Name="lblSubject" FontSize="16" TextWrapping="Wrap" Margin="0,0,0,15"/>

                                    <Grid Margin="0,0,0,15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Priority" FontSize="12" FontWeight="SemiBold" Opacity="0.68" Margin="0,0,0,4"/>
                                            <materialDesign:Chip x:Name="lblPriority" Content="Low" HorizontalAlignment="Left" IsEnabled="False"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="Status" FontSize="12" FontWeight="SemiBold" Opacity="0.68" Margin="0,0,0,4"/>
                                            <TextBlock x:Name="lblStatus" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                        </StackPanel>
                                    </Grid>

                                    <TextBlock Text="Created Date" FontSize="12" FontWeight="SemiBold" Opacity="0.68" Margin="0,0,0,2"/>
                                    <TextBlock x:Name="lblCreatedDate" FontSize="15" Margin="0,0,0,15"/>

                                    <TextBlock Text="Assigned Admin" FontSize="12" FontWeight="SemiBold" Opacity="0.68" Margin="0,0,0,2"/>
                                    <TextBlock x:Name="lblAssigned" FontSize="15" Margin="0,0,0,20"/>


                                    <TextBox x:Name="txtDescription" Style="{StaticResource MaterialDesignOutlinedTextBox}" IsReadOnly="True" 
                                         materialDesign:HintAssist.Hint="Full Description" TextWrapping="Wrap" AcceptsReturn="True" Height="90" VerticalScrollBarVisibility="Auto"/>
                                </StackPanel>
                            </StackPanel>
                        </materialDesign:Card>

                        <materialDesign:Card x:Name="AdminPanel" materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="8" Visibility="Collapsed" Margin="0,0,0,20">
                            <StackPanel>
                                <materialDesign:ColorZone Mode="SecondaryMid" Padding="16" CornerRadius="8,8,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="ShieldAccount" Width="20" Height="20" Foreground="{DynamicResource SecondaryHueMidForegroundBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="Admin Controls" FontWeight="Bold" FontSize="16" Foreground="{DynamicResource SecondaryHueMidForegroundBrush}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </materialDesign:ColorZone>

                                <Grid Margin="20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="20"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" VerticalAlignment="Bottom">
                                        <ComboBox x:Name="cmbStatus" Style="{StaticResource MaterialDesignFloatingHintComboBox}" materialDesign:HintAssist.Hint="Change Status" Margin="0,0,0,20">
                                            <ComboBoxItem Content="Open"/>
                                            <ComboBoxItem Content="In Progress"/>
                                            <ComboBoxItem Content="Closed"/>
                                        </ComboBox>
                                        <Button Content="UPDATE STATUS" Style="{StaticResource MaterialDesignRaisedButton}" Click="BtnUpdateStatus_Click"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" VerticalAlignment="Bottom">
                                        <ComboBox x:Name="cmbAssignAdmin" Style="{StaticResource MaterialDesignFloatingHintComboBox}" 
                                          materialDesign:HintAssist.Hint="Assign To (Admin User)" Margin="0,0,0,20"
                                          DisplayMemberPath="Username" 
                                          SelectedValuePath="UserId">
                                        </ComboBox>
                                        <Button Content="ASSIGN TICKET" Style="{StaticResource MaterialDesignOutlinedButton}" Click="BtnAssign_Click"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </materialDesign:Card>
                    </StackPanel>
                </ScrollViewer>

                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="1.5*"/>
                    </Grid.RowDefinitions>

                    <materialDesign:Card Grid.Row="0" Margin="0,0,0,20" materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="8">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top" Text="Status History" FontSize="18" FontWeight="Bold" Margin="20,20,20,10" Opacity="0.8"/>
                            <DataGrid x:Name="dgHistory" AutoGenerateColumns="False" IsReadOnly="True" Style="{StaticResource MaterialDesignDataGrid}" BorderThickness="0" Margin="10,0,10,10">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Old Status" Binding="{Binding OldStatus}" Width="100"/>
                                    <DataGridTextColumn Header="New Status" Binding="{Binding NewStatus}" Width="100"/>
                                    <DataGridTextColumn Header="Timestamp" Binding="{Binding ChangedAt, StringFormat='\{0:MM/dd/yyyy HH:mm\}'}" Width="150"/>
                                    <DataGridTextColumn Header="Admin ID" Binding="{Binding ChangedByUserId}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </DockPanel>
                    </materialDesign:Card>

                    <materialDesign:Card Grid.Row="1" materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="8">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <materialDesign:ColorZone Mode="Standard" Padding="20,15" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1">
                                <TextBlock Text="Ticket Discussion" FontSize="18" FontWeight="Bold" Opacity="0.8"/>
                            </materialDesign:ColorZone>

                            <ListBox x:Name="lstComments" Grid.Row="1" ScrollViewer.VerticalScrollBarVisibility="Auto" Margin="10" HorizontalContentAlignment="Stretch" BorderThickness="0">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <materialDesign:Card materialDesign:ElevationAssist.Elevation="Dp1" UniformCornerRadius="8" Padding="15" Margin="5,5,5,10">
                                            <StackPanel>
                                                <TextBlock Text="{Binding CommentText}" TextWrapping="Wrap" FontSize="15" Margin="0,0,0,10"/>
                                                <StackPanel Orientation="Horizontal" Opacity="0.6">
                                                    <materialDesign:PackIcon Kind="ClockOutline" Width="14" Height="14" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding CreatedAt, StringFormat='\{0:g\}'}" FontSize="12" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                                    <materialDesign:Chip Content="INTERNAL NOTE" Visibility="{Binding IsInternalVisibility}" FontSize="10" Height="20" Background="#F8D7DA" Foreground="#721C24" IsEnabled="False"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </materialDesign:Card>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <materialDesign:ColorZone Grid.Row="2" Mode="Standard" Padding="15" materialDesign:ElevationAssist.Elevation="Dp4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="txtNewComment" Grid.Column="0" Style="{StaticResource MaterialDesignOutlinedTextBox}" materialDesign:HintAssist.Hint="Type your message here..." TextWrapping="Wrap" AcceptsReturn="True" MaxHeight="100" VerticalScrollBarVisibility="Auto" Margin="0,0,15,0"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Bottom">
                                        <CheckBox x:Name="chkIsInternal" Content="Internal" Margin="0,0,0,10" Visibility="Collapsed" HorizontalAlignment="Center"/>
                                        <Button Style="{StaticResource MaterialDesignFloatingActionMiniButton}" Click="BtnAddComment_Click" ToolTip="Post Comment">
                                            <materialDesign:PackIcon Kind="Send" Width="20" Height="20"/>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </materialDesign:ColorZone>
                        </Grid>
                    </materialDesign:Card>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>