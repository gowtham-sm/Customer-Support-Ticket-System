<Window x:Class="SupportTicketClient.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Support Ticket Dashboard" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen" Loaded="Window_Loaded"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular" TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal" TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Border Background="{DynamicResource MaterialDesignPaper}" CornerRadius="12" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="4" Opacity="0.2" Direction="270"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource MaterialDesignTextFieldBoxBackground}" CornerRadius="12,0,0,12">
                <Grid Margin="0,20,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="25,10,25,40" Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Lifebuoy" Width="32" Height="32" Foreground="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="SupportDesk" FontSize="22" FontWeight="Bold" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Margin="15,0">
                        <Button Style="{StaticResource MaterialDesignFlatButton}" Height="50" HorizontalContentAlignment="Left" Foreground="{DynamicResource PrimaryHueMidBrush}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ViewDashboard" Width="24" Height="24" Margin="0,0,15,0"/>
                                <TextBlock Text="All Tickets" FontSize="16" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="btnRefresh" Style="{StaticResource MaterialDesignFlatButton}" Height="50" HorizontalContentAlignment="Left" Click="BtnRefresh_Click" Opacity="0.7">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Refresh" Width="24" Height="24" Margin="0,0,15,0"/>
                                <TextBlock Text="Sync Database" FontSize="16" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Margin="15,0">
                        <Separator Margin="10,0,10,20" Background="{DynamicResource MaterialDesignDivider}"/>
                        <Button x:Name="btnTheme" Style="{StaticResource MaterialDesignFlatButton}" Height="50" HorizontalContentAlignment="Left" Click="BtnTheme_Click" Opacity="0.7">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ThemeLightDark" Width="24" Height="24" Margin="0,0,15,0"/>
                                <TextBlock Text="Toggle Theme" FontSize="16" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="btnLogout" Style="{StaticResource MaterialDesignFlatButton}" Height="50" HorizontalContentAlignment="Left" Click="BtnLogout_Click" Foreground="#F44336">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Logout" Width="24" Height="24" Margin="0,0,15,0"/>
                                <TextBlock Text="Sign Out" FontSize="16" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Background="Transparent" MouseDown="DragWindow_MouseDown">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,15,0">
                        <Button Style="{StaticResource MaterialDesignFlatButton}" Width="40" Height="40" Padding="0" Click="BtnMinimize_Click" Foreground="{DynamicResource MaterialDesignBody}">
                            <materialDesign:PackIcon Kind="WindowMinimize" Width="20" Height="20" VerticalAlignment="Bottom" Margin="0,0,0,10"/>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignFlatButton}" Width="40" Height="40" Padding="0" Click="BtnClose_Click" Foreground="{DynamicResource MaterialDesignBody}">
                            <materialDesign:PackIcon Kind="WindowClose" Width="20" Height="20"/>
                        </Button>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1" Margin="30,10,30,30">
                    <TextBlock Text="Active Tickets" FontSize="28" FontWeight="Bold" Margin="0,0,0,20" VerticalAlignment="Top"/>

                    <materialDesign:Card materialDesign:ElevationAssist.Elevation="Dp1" UniformCornerRadius="8" Margin="0,50,0,0">
                        <DataGrid x:Name="dgTickets" AutoGenerateColumns="False" IsReadOnly="True" 
                                  SelectionMode="Single" MouseDoubleClick="DgTickets_MouseDoubleClick"
                                  Style="{StaticResource MaterialDesignDataGrid}"
                                  BorderThickness="0" Margin="10" RowHeight="55">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding TicketId}" Width="60"/>
                                <DataGridTextColumn Header="Subject" Binding="{Binding Subject}" Width="*"/>

                                <DataGridTemplateColumn Header="Priority" Width="130">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <materialDesign:Chip Content="{Binding Priority}" FontSize="12" Height="26" IsEnabled="False"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTemplateColumn Header="Status" Width="140">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="{Binding Status, Converter={StaticResource StatusColorConverter}}" 
                                                    CornerRadius="13" Padding="14,6" HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding Status}" Foreground="White" FontWeight="Bold" FontSize="12"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="Assigned Admin" Binding="{Binding AssignedToName, TargetNullValue='Unassigned'}" Width="140"/>

                                <DataGridTextColumn Header="Created Date" Binding="{Binding CreatedDate, StringFormat='\{0:MMM dd, yyyy HH:mm\}'}" Width="160"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </materialDesign:Card>

                    <Button x:Name="btnCreateTicket" 
                            Style="{StaticResource MaterialDesignFloatingActionSecondaryButton}" 
                            HorizontalAlignment="Right" VerticalAlignment="Bottom" 
                            Margin="0,0,20,20" Width="60" Height="60"
                            materialDesign:ElevationAssist.Elevation="Dp4"
                            Click="BtnCreateTicket_Click" ToolTip="Create New Ticket">
                        <materialDesign:PackIcon Kind="Plus" Width="30" Height="30"/>
                    </Button>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>