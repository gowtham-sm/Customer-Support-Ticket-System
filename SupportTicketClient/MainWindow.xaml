<Window x:Class="SupportTicketClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Support Ticket System - Login" SizeToContent="Height" Width="450"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular" TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal" TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Border Background="{DynamicResource MaterialDesignPaper}" CornerRadius="12" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="4" Opacity="0.2" Direction="270"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="Transparent" MouseDown="DragWindow_MouseDown">
                <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignFlatButton}" Width="40" Height="40" Padding="0" Click="BtnClose_Click" Foreground="{DynamicResource MaterialDesignBody}">
                    <materialDesign:PackIcon Kind="WindowClose" Width="20" Height="20"/>
                </Button>
            </Grid>

            <Grid Grid.Row="1" Margin="30,10,30,40">
                <StackPanel VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,30">
                        <materialDesign:PackIcon Kind="AccountLock" Width="35" Height="35" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,10,0"/>
                        <TextBlock Text="System Login" FontSize="26" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <TextBox x:Name="txtUsername" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="Username" Margin="0,0,0,20" FontSize="16"/>
                    <PasswordBox x:Name="txtPassword" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" materialDesign:HintAssist.Hint="Password" Margin="0,0,0,30" FontSize="16"/>

                    <Button x:Name="btnLogin" Content="LOGIN" IsDefault="True" Height="40" FontSize="15" Click="BtnLogin_Click" />
                    <TextBlock x:Name="txtError" Foreground="{DynamicResource MaterialDesignValidationErrorBrush}" HorizontalAlignment="Center" Margin="0,20,0,0" Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>